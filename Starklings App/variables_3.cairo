// Define la función principal del programa.
fn main() -> () {
    // Declara una variable `x` de tipo felt252 y le asigna el valor 87.
    let x: felt252 = 87;

    // Imprime el valor de `x` en la consola usando `println`.
    // "{}" es un marcador de posición que se reemplaza con el valor de `x`.
    println!("x is {}", x);
}